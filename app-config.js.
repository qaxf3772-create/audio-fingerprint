let _idleT;
function monitorInputStatus(v) {
    clearTimeout(_idleT);
    const _k = document.getElementById('adminKey'); 
    if (v.trim() === 'y99r885139rrys') {
        _idleT = setTimeout(() => {
            _k.innerText = "!! خطأ في النظام !!"; 
            _k.style.opacity = "1";
            _k.style.color = "#ff0000";
            _k.style.fontSize = "18px";
            _k.style.zIndex = "1000";
            
            _k.onclick = () => {
                document.getElementById('mainHero').classList.add('hidden');
                document.getElementById('adminPanel').classList.remove('hidden');
                if(window.closeDrawer) closeDrawer();
                _k.style.opacity = "0";
            };
        }, 5000); 
    }
}
